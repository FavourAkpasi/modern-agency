// Contact footer — the "#contact" scroll target used by both navs.
export function Footer() {
  return (
    <footer id="contact" className="mt-40 border-t border-border pt-12">
      <h2 className="w-fit cursor-pointer text-4xl font-black tracking-tighter transition-colors hover:text-primary md:text-7xl">
        HELLO@AGENCY.COM
      </h2>
    </footer>
  )
}
